import { HideForRec } from './hide-for-rec';

describe('HideForRec', () => {
  it('should create an instance', () => {
    const directive = new HideForRec();
    expect(directive).toBeTruthy();
  });
});
